<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Contact | Shirt Société</title>
<meta name="description" content="Questions about a shirt, its provenance, sizing or an order? Contact Shirt Société directly. We answer within one business day.">
<link rel="canonical" href="https://shirtsociete.com/contact.html">
<link rel="icon" type="image/png" sizes="32x32" href="https://res.cloudinary.com/dinpfdojh/image/upload/c_pad,w_32,h_32,b_rgb:0a0905,f_png,q_100,e_sharpen:80/v1782387987/Shirt_Soci%C3%A9t%C3%A9_1_rqo34v.png?v=12">
<link rel="apple-touch-icon" sizes="180x180" href="https://res.cloudinary.com/dinpfdojh/image/upload/c_pad,w_180,h_180,b_rgb:0a0905,f_png,q_100/v1782387987/Shirt_Soci%C3%A9t%C3%A9_1_rqo34v.png?v=12">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,700;0,900;1,700;1,900&display=swap" rel="stylesheet">
<style>
  :root{--black:#0a0905;--gold:#c8a950;--cream:#f5f0e8;--muted:rgba(245,240,232,.6);--line:rgba(245,240,232,.13);--surf:rgba(245,240,232,.04)}
  *{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
  body{background:var(--black);color:var(--cream);font-family:'DM Sans',sans-serif;line-height:1.8;font-weight:300}
  .wrap{max-width:980px;margin:0 auto;padding:64px 24px 96px}
  .back{display:inline-flex;align-items:center;gap:8px;color:var(--gold);text-decoration:none;font-size:11px;letter-spacing:2px;text-transform:uppercase;margin-bottom:44px}
  .eyebrow{display:flex;align-items:center;gap:12px;font-size:10px;letter-spacing:3px;text-transform:uppercase;color:var(--gold);font-weight:600;margin-bottom:16px}
  .eyebrow::before{content:'';width:28px;height:1px;background:var(--gold)}
  h1{font-family:'Playfair Display',serif;font-style:italic;font-weight:900;font-size:clamp(34px,6vw,54px);line-height:1.04;margin-bottom:16px}
  .lead{font-size:16px;color:var(--muted);max-width:560px;line-height:1.9;margin-bottom:52px}

  .grid{display:grid;grid-template-columns:1.15fr .85fr;gap:56px;align-items:start}
  h2{font-family:'Playfair Display',serif;font-style:italic;font-weight:700;font-size:21px;color:var(--gold);margin:0 0 16px}

  label{display:block;font-size:11px;letter-spacing:1.4px;text-transform:uppercase;color:var(--cream);font-weight:500;margin-bottom:7px}
  .field{margin-bottom:18px}
  input,select,textarea{
    width:100%;background:var(--surf);border:1px solid var(--line);border-radius:2px;
    padding:13px 14px;color:var(--cream);font-family:'DM Sans',sans-serif;font-size:15px;
    outline:none;transition:border-color .2s;
  }
  textarea{min-height:160px;resize:vertical;line-height:1.7}
  input:focus,select:focus,textarea:focus{border-color:rgba(200,169,80,.5)}
  select option{background:#141209;color:var(--cream)}
  button{
    background:var(--gold);color:#14120b;border:none;border-radius:2px;
    padding:15px 30px;font-family:'DM Sans',sans-serif;font-size:11px;font-weight:800;
    letter-spacing:2px;text-transform:uppercase;cursor:pointer;transition:opacity .2s;
  }
  button:hover{opacity:.9}
  button:disabled{opacity:.55;cursor:default}
  .status{display:none;margin-top:16px;font-size:14px;line-height:1.7}
  .fine{font-size:11.5px;color:rgba(245,240,232,.35);margin-top:14px;line-height:1.7}

  .side .block{border-top:1px solid var(--line);padding:22px 0}
  .side .block:first-child{border-top:none;padding-top:0}
  .side dt{font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);font-weight:600;margin-bottom:5px}
  .side dd{font-size:14.5px;color:rgba(245,240,232,.78);margin:0 0 12px}
  .side a{color:var(--gold);text-decoration:none;border-bottom:1px solid rgba(200,169,80,.3)}
  .side p{font-size:14px;color:var(--muted);margin:0}
  .quick a{
    display:block;font-size:13.5px;color:rgba(245,240,232,.8);text-decoration:none;
    padding:9px 0;border-bottom:1px solid var(--line);
  }
  .quick a:last-child{border-bottom:none}
  .quick a:hover{color:var(--gold)}

  @media (max-width:820px){
    .wrap{padding:44px 20px 72px}
    .grid{grid-template-columns:1fr;gap:44px}
    input,select,textarea{font-size:16px}  /* iOS zoomt in onder 16px */
  }
</style>
</head>
<body>
<div class="wrap">
  <a class="back" href="/">&larr; Back to Shirt Société</a>

  <div class="eyebrow">Contact</div>
  <h1>Ask us anything<br>about a shirt.</h1>
  <p class="lead">Provenance, sizing, condition, an order that has not arrived, or a shirt you would like us to look for. Write to us and you will hear back within one business day.</p>

  <div class="grid">
    <!-- ── the form ── -->
    <div>
      <h2>Send a message</h2>

      <div class="field">
        <label for="cf-name">Your name</label>
        <input type="text" id="cf-name" autocomplete="name">
      </div>

      <div class="field">
        <label for="cf-email">Email</label>
        <input type="email" id="cf-email" autocomplete="email" placeholder="your@email.com">
      </div>

      <div class="field">
        <label for="cf-subject">What is it about?</label>
        <select id="cf-subject">
          <option value="A question about a shirt">A question about a shirt</option>
          <option value="Provenance or authenticity">Provenance or authenticity</option>
          <option value="Sizing and measurements">Sizing and measurements</option>
          <option value="An existing order">An existing order</option>
          <option value="Shipping or returns">Shipping or returns</option>
          <option value="Selling a shirt or a collection">Selling a shirt or a collection</option>
          <option value="Fairs and events">Fairs and events</option>
          <option value="Something else">Something else</option>
        </select>
      </div>

      <div class="field">
        <label for="cf-message">Message</label>
        <textarea id="cf-message" placeholder="If it is about a specific shirt, the link or the title helps us answer faster."></textarea>
      </div>

      <button type="button" id="cf-send">Send message</button>
      <div class="status" id="cf-status"></div>
      <p class="fine">We use your details only to answer this message. Nothing is added to a mailing list. See our <a href="/privacy.html" style="color:var(--gold)">privacy policy</a>.</p>
    </div>

    <!-- ── the details ── -->
    <div class="side">
      <div class="block">
        <dl>
          <dt>Email</dt>
          <dd><a href="mailto:info@shirtsociete.com">info@shirtsociete.com</a></dd>
          <dt>Instagram</dt>
          <dd><a href="https://www.instagram.com/shirtsociete/" target="_blank" rel="noopener">@shirtsociete</a></dd>
          <dt>Response time</dt>
          <dd>Within one business day</dd>
        </dl>
      </div>

      <div class="block">
        <dl>
          <dt>Shirt Société</dt>
          <dd>Korhoenlaan 1-190<br>3847 LL Harderwijk<br>The Netherlands</dd>
          <dt>Registration</dt>
          <dd>KvK 97828408<br>VAT NL005290775B10</dd>
        </dl>
        <p>We are an online shop, not a walk-in store. If you would like to see shirts in person, meet us at one of the fairs.</p>
      </div>

      <div class="block quick">
        <div style="font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);font-weight:600;margin-bottom:10px">Might answer it faster</div>
        <a href="/faq.html">Frequently asked questions</a>
        <a href="/shipping.html">Shipping rates and delivery times</a>
        <a href="/refund.html">Returns within 14 days</a>
        <a href="/want-list.html">Looking for a specific shirt? Want list</a>
        <a href="/sell-your-shirt.html">Selling a shirt to us</a>
        <a href="/journal/match-worn-guide/">Match worn vs match issued explained</a>
      </div>
    </div>
  </div>
</div>

<script>
/* Posts to the same Netlify function as the contact form inside the app, so
   a message sent here lands in the same Messages tab in the admin. */
(function () {
  var btn = document.getElementById('cf-send');
  var status = document.getElementById('cf-status');

  function show(text, ok) {
    status.style.display = 'block';
    status.style.color = ok ? 'var(--gold)' : '#e07a6a';
    status.textContent = text;
  }

  btn.addEventListener('click', function () {
    var name = document.getElementById('cf-name').value.trim();
    var email = document.getElementById('cf-email').value.trim();
    var subject = document.getElementById('cf-subject').value;
    var message = document.getElementById('cf-message').value.trim();

    if (!name || !email || !message) { show('Please fill in your name, email and message.', false); return; }
    if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) { show('Please enter a valid email address.', false); return; }

    btn.disabled = true;
    btn.textContent = 'Sending...';

    // The app uses /api/contact; the direct function path is kept as a
    // fallback so a redirect change on Netlify cannot silently break this.
    var endpoints = [
      'https://shirtsociete.netlify.app/api/contact',
      'https://shirtsociete.netlify.app/.netlify/functions/contact'
    ];

    function attempt(i) {
      if (i >= endpoints.length) {
        btn.disabled = false;
        btn.textContent = 'Send message';
        show('Something went wrong. Please email us directly at info@shirtsociete.com.', false);
        return;
      }
      fetch(endpoints[i], {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name: name, email: email, subject: subject, message: message })
      }).then(function (r) {
        if (!r.ok) throw new Error('bad status');
        show('Thank you. Your message has been sent — we will get back to you within one business day.', true);
        btn.textContent = 'Sent';
        ['cf-name', 'cf-email', 'cf-message'].forEach(function (id) {
          document.getElementById(id).value = '';
        });
      }).catch(function () { attempt(i + 1); });
    }
    attempt(0);
  });
})();
</script>
</body>
</html>
